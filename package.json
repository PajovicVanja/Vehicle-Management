{
  "name": "vehicle-management",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "body-parser": "^2.2.1",
    "cloudinary": "^2.5.1",
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "firebase-admin": "^12.7.0",
    "formidable": "^3.5.0"
  },
  "scripts": {
    "vercel-build": "npm --prefix frontend ci && npm --prefix frontend run build",
    "build:frontend": "npm --prefix frontend ci && npm --prefix frontend run build",
    "test": "jest",
    "test:api": "jest",
    "test:frontend": "npm --prefix frontend test -- --watchAll=false",
    "start:local": "concurrently \"node local-server.js\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "jest": "^29.7.0",
    "node-mocks-http": "^1.11.0"
  }
}